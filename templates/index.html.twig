{% extends 'base.html.twig' %}

{% block body %}
	<h1>Liste des livres</h1>

	<ul>
		{% for livre in livres %}
			<li>{{ livre.titre }}
				-
				{{ livre.auteur }}</li>
		{% endfor %}
	</ul>
{% endblock %}
